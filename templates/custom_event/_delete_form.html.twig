<form method="post" action="{{ path('app_custom_event_delete', {'id': custom_event.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ custom_event.id) }}">
    <button class="btn">Delete</button>
</form>
