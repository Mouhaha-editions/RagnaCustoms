<h5 class="text-info mb-2">{{ "%song_name% by %artist%"|trans|replace({"%song_name%": song.name,"%artist%": song.authorName}) }}</h5>
{% form_theme form 'bootstrap_4_layout.html.twig' %}

{{ form_start(form) }}
<div class="row">
    <div class="col-md-6">
        {{ form_row(form.funFactor) }}
        {{ form_row(form.rhythm) }}
        {{ form_row(form.patternQuality) }}
        {{ form_row(form.readability) }}
    </div>
</div>

{{ form_end(form) }}
