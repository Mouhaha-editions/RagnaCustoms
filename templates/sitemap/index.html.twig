<?xml version="1.0" encoding="UTF-8"?>
    <sitemapindex xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
    {% for page in 0..songs %}
        <sitemap>
            <loc>{{ url('sitemap_songs_page',{page:page}) }}</loc>
        </sitemap>
{% endfor %}
    {% for page in 0..artists %}
        <sitemap>
            <loc>{{ url('sitemap_artists_page',{page:page}) }}</loc>
        </sitemap>
    {% endfor %}
</sitemapindex>
